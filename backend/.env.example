# Copiar a .env y ajustar. NUNCA commitear el .env real.
DATABASE_URL="postgresql://matriz:matriz_dev_local@localhost:5432/matriz_sgr?schema=public"
JWT_SECRET="cambiar-por-secreto-largo-aleatorio"
JWT_EXPIRES_IN="8h"
PORT=4000
CORS_ORIGIN="http://localhost:5173"
# Frecuencia del recálculo de cumplimiento (cron, cada 10 min por defecto)
CRON_CUMPLIMIENTO="*/10 * * * *"
