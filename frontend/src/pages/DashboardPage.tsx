// Placeholder: los dashboards BI (gauges, heatmap, radar, KPIs con ECharts)
// se construyen en la Fase 4 sobre GET /kpis/cumplimiento.
export function DashboardPage() {
  return (
    <div>
      <h2>Dashboard</h2>
      <p className="vacio">
        Los semáforos, heatmaps y KPIs se habilitan en la Fase 4 (BI y Dashboards).
      </p>
    </div>
  );
}
